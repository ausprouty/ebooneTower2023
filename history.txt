nvm use 16.18.1
nvm current (to show what you are using)
use xampp to left (xampp73)
npm run serve-mc2
npm run build-mc2
npm run serve-intro
   please delete node modules unless you are sure you have the right styles in here
zip

I am working on Series Mixin

// Set book format
store.commit('setBookProperty', { index: 1, property: 'format', value: 'hardcover' });


/ Get format back button
const backButton = store.getters.getLibraryProperty('format.back_button');

// Get format custom
const custom = store.getters.getLibraryProperty('format.custom');

// Get format image
const image = store.getters.getLibraryProperty('format.image');

// Get format no ribbon
const noRibbon = store.getters.getLibraryProperty('format.no_ribbon');

// Get format replace header
const replaceHeader = store.getters.getLibraryProperty('format.replace_header');


installed nvm by going to https://github.com/coreybutler/nvm-windows
You need to restart the machine for nvm to be seen.

Installed nodejs 18.12.1 (as per ampp)
Installed nodejs 16.18.1 (as per xamp73)
npx browserslist@latest --update-db
npm install --save-dev @vue/test-utils jest   for testing

Extensions:
    eslint
    flat ui
    prettier
    Vetur
    Volar

